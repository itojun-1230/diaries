{
  "name": "diaries",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "node esbuild.js",
    "push": "clasp push",
    "open": "clasp open",
    "deploy": "yarn build && clasp push"
  },
  "devDependencies": {
    "@google/clasp": "^2.5.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "esbuild-gas-plugin": "^0.8.0"
  }
}
